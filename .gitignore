# build output
dist/
# generated types
.astro/
# notion cache
.cache/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# local dev files
.playwright-mcp/
aurabuild.html
refonte.html
profile.md
retoursv1.md
todo.md
test-results/
screenshots/
mobile-audit.spec.ts
timeline-check.spec.ts
NOTES-SESSION.md
.vercel

# WIP local : maquettes Pencil + scratchpad screenshots (ne doivent pas voyager dans le repo)
*.pen
images/

# Impeccable design tooling (plugin skill + hooks + live config) — installé en local, hors repo
.claude/skills/
.impeccable/

# Lock de scheduler de session (transitoire, ne pas versionner)
.claude/scheduled_tasks.lock

# Artefacts QA agent-browser (captures, logs console, arbres a11y) — locaux
.playwright-cli/
output/
hero.png

# Outillage impeccable côté Codex : installé en local, jamais versionné.
# Il pesait 137 fichiers dans le dépôt, dont un script de 459 Ko.
.agents/skills/

# Captures de QA à la racine. Les six lignes nominatives d'avant ne
# rattrapaient que les noms déjà connus : un motif les couvre toutes.
*.png
!public/*.png
!src/**/*.png
