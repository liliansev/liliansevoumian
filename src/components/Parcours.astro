---
import { Image } from 'astro:assets';
import { Icon } from 'astro-icon/components';
import lilianPortrait from '../assets/lilian-portrait.png';

const timeline = [
  {
    year: "2026",
    badge: "Aujourd'hui",
    title: "Consultant IA & Automatisation",
    description: "Lancement de formation-n8n.com. J'accompagne les entreprises dans leur optimisation et la mise en place d'IA.",
    isCurrent: true
  },
  {
    year: "2024",
    badge: "Partner Make",
    title: "Co-fondation de La Capsule Studio",
    description: "Studio podcast à Paris. Automatisation complète : réservations, facturation, planning, delivery."
  },
  {
    year: "2022",
    badge: "Certification 5",
    title: "Jellysmack & Accélération",
    description: "No-Code Engineer. Lancement d'apprendre-make.com.",
    link: "https://partnertraining.make.com/certificates/7qip5uhbat"
  },
  {
    year: "2021",
    title: "Lancement freelance",
    description: "Premiers clients : Qonto, KlaK, WeTechCare."
  }
];

const passions = [
  { name: "Podcasts", icon: "mdi:headphones" },
  { name: "Gaming", icon: "mdi:gamepad-variant" },
  { name: "Voyages", icon: "mdi:airplane" }
];
---

<section id="parcours" class="bg-zinc-900 py-24 px-6">
  <div class="max-w-5xl mx-auto">
    <div class="grid md:grid-cols-[1fr_280px] gap-12 md:gap-16">
      <!-- Timeline Column -->
      <div>
        <!-- Quote -->
        <p class="text-zinc-500 text-sm italic mb-8 max-w-md">
          "Plus jeune, je passais des heures à min-maxer mes persos sur WoW et Final Fantasy. Aujourd'hui, c'est pareil — sauf que j'optimise des business au lieu de stats d'attaque."
        </p>

        <!-- Header -->
        <div class="mb-10">
          <h2 class="fluid-h1 font-semibold text-white mb-3">
            5 ans d'automatisation
          </h2>
          <p class="text-zinc-400 fluid-text-base">
            De freelance à expert top 5 mondial Make.
          </p>
        </div>

        <!-- Timeline -->
        <div class="space-y-6">
          {timeline.map((item, index) => (
            <div class="flex gap-4 group">
              <!-- Year -->
              <div class="flex flex-col items-end w-16 shrink-0 pt-0.5">
                <span class={`text-sm font-semibold ${item.isCurrent ? 'text-white' : 'text-zinc-500'}`}>
                  {item.year}
                </span>
                {item.badge && item.link ? (
                  <a href={item.link} target="_blank" rel="noopener" class="text-[10px] text-zinc-500 hover:text-zinc-300 font-mono mt-0.5 underline underline-offset-2 whitespace-nowrap">{item.badge}</a>
                ) : item.badge && (
                  <span class="text-[10px] text-zinc-500 font-mono mt-0.5 whitespace-nowrap">{item.badge}</span>
                )}
              </div>

              <!-- Content -->
              <div>
                <h3 class="fluid-text-base font-semibold text-white mb-1 group-hover:text-zinc-300 transition-colors">
                  {item.title}
                </h3>
                <p class="text-zinc-400 text-sm leading-relaxed">
                  {item.description}
                </p>
              </div>
            </div>
          ))}
        </div>
      </div>

      <!-- About Column -->
      <div class="flex flex-col items-center md:items-start">
        <!-- Photo -->
        <div class="w-48 md:w-full aspect-square rounded-2xl overflow-hidden shadow-xl ring-1 ring-zinc-700 mb-6">
          <Image
            src={lilianPortrait}
            alt="Lilian Sevoumian"
            width={280}
            height={280}
            format="webp"
            quality={85}
            class="w-full h-full object-cover object-top -scale-x-100"
            loading="lazy"
          />
        </div>

        <!-- Quote -->
        <p class="text-zinc-400 text-sm italic text-center md:text-left leading-relaxed mb-6">
          "Quand je me balade quelque part, je vois tout ce qu'on pourrait automatiser. C'est plus fort que moi."
        </p>

        <!-- Passions -->
        <div class="flex flex-wrap gap-2 justify-center md:justify-start">
          {passions.map((passion) => (
            <span class="flex items-center gap-1.5 px-3 py-1.5 bg-zinc-800 rounded-full text-xs text-zinc-400">
              <Icon name={passion.icon} class="w-3.5 h-3.5" />
              {passion.name}
            </span>
          ))}
        </div>
      </div>
    </div>
  </div>
</section>
