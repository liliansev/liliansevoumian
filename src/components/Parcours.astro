---
import { Icon } from 'astro-icon/components';
import AboutCard from './ui/AboutCard.astro';

const timeline = [
  {
    year: "2026",
    badge: "Aujourd'hui",
    title: "Consultant IA & Automatisation",
    description: "Lancement de formation-n8n.com. J'accompagne les entreprises dans leur optimisation et la mise en place d'IA.",
    isCurrent: true
  },
  {
    year: "2024",
    badge: "Partner Make",
    title: "Co-fondation de La Capsule Studio",
    description: "Studio podcast à Paris. Automatisation complète : réservations, facturation, planning, delivery."
  },
  {
    year: "2022",
    badge: "Certif\u00A05",
    title: "Jellysmack & Accélération",
    description: "No-Code Engineer. Lancement d'apprendre-make.com.",
    link: "https://partnertraining.make.com/certificates/7qip5uhbat"
  },
  {
    year: "2021",
    title: "Lancement freelance",
    description: "Premiers clients : Qonto, KlaK, WeTechCare."
  }
];

---

<section id="parcours" class="bg-zinc-100 section-padding px-6">
  <div class="max-w-5xl mx-auto">
    <div class="grid lg:grid-cols-[1fr_280px] gap-12 lg:gap-16">
      <!-- Timeline Column -->
      <div>
        <!-- Quote -->
        <p class="text-zinc-500 fluid-text-sm italic mb-8 max-w-md">
          "Plus jeune, je passais des heures à min-maxer mes persos sur WoW et Final Fantasy. Aujourd'hui, c'est pareil — sauf que j'optimise des business au lieu de stats d'attaque."
        </p>

        <!-- Header -->
        <div class="mb-10">
          <h2 class="fluid-h1 font-semibold text-zinc-900 mb-3">
            5&nbsp;ans d'automatisation
          </h2>
          <p class="text-zinc-500 fluid-text-base">
            De freelance à expert top&nbsp;5 mondial Make.
          </p>
        </div>

        <!-- Photo + Quote + Passions - Mobile Only -->
        <AboutCard variant="mobile" />

        <!-- Timeline -->
        <div class="space-y-6">
          {timeline.map((item, index) => (
            <div class="flex gap-4 group">
              <!-- Year -->
              <div class="flex flex-col items-end w-16 shrink-0 pt-0.5">
                <span class={`fluid-text-sm font-semibold ${item.isCurrent ? 'text-zinc-900' : 'text-zinc-500'}`}>
                  {item.year}
                </span>
                {item.badge && (
                  item.link ? (
                    <a href={item.link} target="_blank" rel="noopener noreferrer" class="fluid-text-xs text-zinc-500 hover:text-zinc-900 font-mono mt-0.5 underline underline-offset-2">{item.badge}</a>
                  ) : (
                    <span class="fluid-text-xs text-zinc-500 font-mono mt-0.5">{item.badge}</span>
                  )
                )}
              </div>

              <!-- Content -->
              <div>
                <h3 class="fluid-text-base font-semibold text-zinc-900 mb-1 group-hover:text-zinc-700 transition-colors">
                  {item.title}
                </h3>
                <p class="text-zinc-500 fluid-text-sm">
                  {item.description}
                </p>
              </div>
            </div>
          ))}
        </div>
      </div>

      <!-- About Column - Desktop Only -->
      <AboutCard variant="desktop" />
    </div>
  </div>
</section>
