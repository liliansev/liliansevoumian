---
import ThemeToggle from './ThemeToggle.astro';

const navLinks = [
  { href: "#roi", label: "ROI", num: "01" },
  { href: "#cas", label: "Cas", num: "02" },
  { href: "#stack", label: "Stack", num: "03" },
  { href: "#offres", label: "Offres", num: "04" },
  { href: "#faq", label: "FAQ", num: "05" },
];
---

<nav id="main-nav" class="nav-root" aria-label="Navigation principale">
  <div class="max-w-[1200px] mx-auto h-full px-[var(--spacing-section-x)] flex items-center justify-between gap-4">
    <!-- Logo + name -->
    <a
      href="/"
      id="logo-link"
      class="inline-flex items-center gap-2.5 text-[var(--color-ink)] shrink-0 group min-h-[44px]"
      aria-label="Lilian Sevoumian, accueil"
    >
      <svg width="22" height="22" viewBox="0 0 22 22" fill="none" aria-hidden="true" class="shrink-0">
        <rect x="0.5" y="0.5" width="21" height="21" stroke="currentColor" stroke-width="1"/>
        <line x1="11" y1="0" x2="11" y2="22" stroke="currentColor" stroke-width="1"/>
        <line x1="0" y1="11" x2="22" y2="11" stroke="currentColor" stroke-width="1"/>
        <circle cx="11" cy="11" r="3" fill="currentColor"/>
      </svg>
      <span class="mono-label hidden sm:inline">LILIAN SEVOUMIAN</span>
      <span class="mono-label sm:hidden">LS</span>
    </a>

    <!-- Desktop links -->
    <div class="hidden lg:flex items-center gap-1">
      {navLinks.map((link) => (
        <a href={link.href} class="nav-link" data-nav-link>
          {link.label}
        </a>
      ))}
    </div>

    <!-- Desktop CTA -->
    <div class="hidden md:flex items-center gap-2">
      <ThemeToggle />
      <a
        href="#contact"
        class="btn-primary"
        data-fast-goal="cta_contact_click"
        data-fast-goal-source="nav"
      >
        Me contacter
      </a>
    </div>

    <!-- Mobile burger -->
    <button
      id="mobile-menu-btn"
      class="md:hidden min-h-[44px] min-w-[44px] flex flex-col justify-center items-center gap-[5px] text-[var(--color-ink)]"
      aria-label="Ouvrir le menu"
      aria-expanded="false"
      aria-controls="mobile-menu"
    >
      <span class="block w-5 h-px bg-current transition-transform duration-200 origin-center"></span>
      <span class="block w-5 h-px bg-current transition-transform duration-200 origin-center"></span>
      <span class="block w-5 h-px bg-current transition-transform duration-200 origin-center"></span>
    </button>
  </div>
</nav>

<!-- Mobile menu plein écran -->
<div
  id="mobile-menu"
  class="fixed inset-0 z-[49] bg-[var(--color-paper)] opacity-0 invisible transition-opacity duration-150 md:hidden"
  role="dialog"
  aria-modal="true"
  aria-labelledby="mobile-menu-title"
  aria-hidden="true"
>
  <h2 id="mobile-menu-title" class="sr-only">Navigation principale</h2>
  <div class="h-full flex flex-col px-[var(--spacing-section-x)] pt-[80px] pb-12">
    <div class="flex-1 flex flex-col justify-center gap-6">
      {navLinks.map((link) => (
        <a
          href={link.href}
          class="mobile-link group flex items-baseline gap-4 py-2 border-b border-[var(--color-divider)]"
          data-mobile-link
        >
          <span class="mono-caption text-[var(--color-ink-low)] w-8">{link.num}</span>
          <span class="font-display font-semibold text-[var(--text-headline)] leading-[1.05] tracking-[-0.035em] text-[var(--color-ink)]">
            {link.label}
          </span>
        </a>
      ))}
    </div>
    <div class="flex flex-col gap-4">
      <div class="flex items-center justify-between border-b border-[var(--color-divider)] pb-3">
        <span class="mono-caption text-[var(--color-ink-low)]">THÈME</span>
        <ThemeToggle />
      </div>
      <a
        href="#contact"
        class="btn-primary justify-center w-full"
        data-mobile-link
        data-fast-goal="cta_contact_click"
        data-fast-goal-source="nav_mobile"
      >
        Me contacter
      </a>
    </div>
  </div>
</div>

<script>
  // Scroll state — borderbottom shifts at scroll > 60px
  const nav = document.getElementById('main-nav');
  function onScroll() {
    if (!nav) return;
    if (window.scrollY > 60) nav.classList.add('scrolled');
    else nav.classList.remove('scrolled');
  }
  window.addEventListener('scroll', onScroll, { passive: true });
  onScroll();

  // Active link tracking via IntersectionObserver
  const navLinks = document.querySelectorAll<HTMLAnchorElement>('[data-nav-link]');
  const sections = Array.from(navLinks)
    .map((a) => {
      const id = a.getAttribute('href')?.replace('#', '') ?? '';
      return id ? document.getElementById(id) : null;
    })
    .filter(Boolean) as HTMLElement[];

  if (sections.length > 0) {
    const obs = new IntersectionObserver((entries) => {
      entries.forEach((e) => {
        if (e.isIntersecting) {
          const id = (e.target as HTMLElement).id;
          navLinks.forEach((a) => {
            const isActive = a.getAttribute('href') === `#${id}`;
            a.classList.toggle('is-active', isActive);
            if (isActive) a.setAttribute('aria-current', 'location');
            else a.removeAttribute('aria-current');
          });
        }
      });
    }, { rootMargin: '-40% 0px -55% 0px', threshold: 0 });
    sections.forEach((s) => obs.observe(s));
  }

  // Mobile menu
  const menuBtn = document.getElementById('mobile-menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');
  const burgerLines = menuBtn?.querySelectorAll('span');

  function toggleMenu(forceClose = false) {
    if (!mobileMenu || !menuBtn) return;
    const willOpen = forceClose ? false : !mobileMenu.classList.contains('is-open');
    mobileMenu.classList.toggle('is-open', willOpen);
    mobileMenu.style.opacity = willOpen ? '1' : '0';
    mobileMenu.style.visibility = willOpen ? 'visible' : 'hidden';
    mobileMenu.setAttribute('aria-hidden', String(!willOpen));
    menuBtn.setAttribute('aria-expanded', String(willOpen));
    document.body.style.overflow = willOpen ? 'hidden' : '';

    if (burgerLines) {
      const [l1, l2, l3] = Array.from(burgerLines);
      if (willOpen) {
        l1?.style.setProperty('transform', 'translateY(6px) rotate(45deg)');
        l2?.style.setProperty('opacity', '0');
        l3?.style.setProperty('transform', 'translateY(-6px) rotate(-45deg)');
      } else {
        l1?.style.removeProperty('transform');
        l2?.style.removeProperty('opacity');
        l3?.style.removeProperty('transform');
      }
    }

    // Focus management : focus 1er link à l'ouverture, retour au burger à la fermeture
    if (willOpen) {
      window.setTimeout(() => {
        const firstLink = mobileMenu.querySelector<HTMLAnchorElement>('[data-mobile-link]');
        firstLink?.focus();
      }, 50);
    } else {
      menuBtn.focus();
    }
  }

  // Focus trap : Tab cycle uniquement dans le menu ouvert
  mobileMenu?.addEventListener('keydown', (e) => {
    if (!mobileMenu.classList.contains('is-open') || e.key !== 'Tab') return;
    const focusable = mobileMenu.querySelectorAll<HTMLElement>('a, button, [tabindex]:not([tabindex="-1"])');
    if (focusable.length === 0) return;
    const first = focusable[0];
    const last = focusable[focusable.length - 1];
    if (e.shiftKey && document.activeElement === first) {
      last.focus();
      e.preventDefault();
    } else if (!e.shiftKey && document.activeElement === last) {
      first.focus();
      e.preventDefault();
    }
  });

  menuBtn?.addEventListener('click', () => toggleMenu());

  mobileMenu?.querySelectorAll('[data-mobile-link]').forEach((link) => {
    link.addEventListener('click', () => {
      setTimeout(() => toggleMenu(true), 100);
    });
  });

  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && mobileMenu?.classList.contains('is-open')) {
      toggleMenu(true);
    }
  });

  // Logo → si on est déjà sur "/", scroll to top sans navigation. Sinon, navigation native.
  document.getElementById('logo-link')?.addEventListener('click', (e) => {
    if (window.location.pathname === '/') {
      e.preventDefault();
      window.scrollTo({ top: 0, behavior: 'smooth' });
      history.replaceState(null, '', window.location.pathname);
    }
  });
</script>
