---
import { Icon } from 'astro-icon/components';
---

<section id="contact" class="text-center py-24 px-6">
  <div class="max-w-3xl mx-auto">
    <!-- Headline -->
    <h2 class="text-3xl md:text-4xl lg:text-5xl font-semibold text-zinc-900 tracking-tight mb-6">
      Combien d'heures perdez-vous <span class="text-zinc-400">chaque semaine</span> ?
    </h2>

    <!-- Subheadline -->
    <p class="text-lg text-zinc-500 mb-10 font-light max-w-xl mx-auto">
      On identifie ensemble ce qui peut être automatisé — et on le fait.
    </p>

    <!-- CTA Button -->
    <div class="flex flex-col items-center gap-6">
      <a
        href="https://cal.com/lilian-sevoumian/prise-de-brief"
        target="_blank"
        rel="noopener"
        class="group relative inline-flex items-center justify-center px-8 py-4 bg-zinc-900 text-white text-sm font-medium rounded shadow-lg shadow-zinc-400/50 hover:shadow-xl hover:shadow-zinc-400/60 hover:-translate-y-0.5 transition-all overflow-hidden"
      >
        <span class="relative z-10 flex items-center gap-2">
          Discuter de mon projet
          <Icon name="lucide:arrow-right" class="w-4 h-4" />
        </span>
        <div class="absolute inset-0 bg-zinc-800 transform scale-x-0 group-hover:scale-x-100 transition-transform origin-left duration-300"></div>
      </a>

      <!-- Badge -->
      <div class="flex items-center gap-4 text-xs text-zinc-400 mt-2">
        <span class="flex items-center gap-1">
          <Icon name="lucide:check-circle" class="w-3.5 h-3.5" />
          Réponse sous 24h
        </span>
      </div>
    </div>
  </div>
</section>
