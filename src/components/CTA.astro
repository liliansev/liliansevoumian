---
import { Image } from 'astro:assets';
import { Icon } from 'astro-icon/components';
import CTAButton from './ui/CTAButton.astro';
import lilianPhoto from '../assets/lilian-cta.png';
---

<section id="contact" class="relative text-center py-24 md:py-32 px-6 overflow-hidden bg-white">
  <!-- Decorative background -->
  <div class="absolute top-0 left-1/4 w-96 h-96 bg-purple-200/20 rounded-full blur-3xl"></div>
  <div class="absolute bottom-0 right-1/4 w-96 h-96 bg-indigo-200/20 rounded-full blur-3xl"></div>

  <div class="relative max-w-3xl mx-auto">
    <!-- Photo -->
    <div class="mb-8">
      <div class="w-20 h-20 mx-auto rounded-full overflow-hidden ring-1 ring-zinc-300 shadow-lg">
        <Image
          src={lilianPhoto}
          alt="Lilian Sevoumian"
          width={160}
          height={160}
          format="webp"
          quality={85}
          class="w-full h-full object-cover"
        />
      </div>
    </div>

    <!-- Headline -->
    <h2 class="fluid-h1 font-semibold text-zinc-900 mb-6">
      Combien d'heures perdez-vous <span class="text-zinc-400">chaque semaine</span> ?
    </h2>

    <!-- Subheadline -->
    <p class="fluid-text-xl text-zinc-500 mb-10 font-light fluid-prose-xl mx-auto leading-relaxed">
      On identifie ensemble ce qui peut être automatisé — et on le fait.<br />
      <strong class="text-zinc-700">Récupérez ces heures dès la semaine prochaine.</strong>
    </p>

    <!-- CTA Button -->
    <div class="flex flex-col items-center gap-6">
      <CTAButton
        href="https://cal.com/lilian-sevoumian/prise-de-brief"
        text="Récupérer mes heures"
        variant="primary"
        size="lg"
      />

      <!-- Badge -->
      <div class="flex flex-col sm:flex-row items-center gap-3 sm:gap-4 text-sm md:text-xs text-zinc-400 mt-2">
        <span class="flex items-center gap-1.5">
          <span class="w-2 h-2 rounded-full bg-emerald-500"></span>
          Créneaux disponibles cette semaine
        </span>
        <span class="hidden sm:inline text-zinc-300">•</span>
        <span class="flex items-center gap-1.5">
          <Icon name="lucide:clock" class="w-4 h-4 md:w-3.5 md:h-3.5" />
          Réponse sous 24h
        </span>
      </div>
    </div>
  </div>
</section>
