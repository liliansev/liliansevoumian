---
import { Picture } from 'astro:assets';
import CTAButton from './ui/CTAButton.astro';
import lilianPhoto from '../assets/lilian-cta.png';
---

<section id="contact" class="relative text-center section-padding px-6 overflow-hidden bg-white">
  <!-- Decorative background -->
  <div class="absolute top-0 left-1/4 w-96 h-96 bg-purple-200/20 rounded-full blur-3xl"></div>
  <div class="absolute bottom-0 right-1/4 w-96 h-96 bg-indigo-200/20 rounded-full blur-3xl"></div>

  <div class="relative max-w-3xl mx-auto">
    <!-- Photo -->
    <div class="mb-8">
      <div class="w-20 h-20 mx-auto rounded-full overflow-hidden ring-1 ring-zinc-300 shadow-lg">
        <Picture
          src={lilianPhoto}
          alt="Lilian Sevoumian"
          width={160}
          height={160}
          formats={['avif', 'webp']}
          quality={85}
          class="w-full h-full object-cover"
        />
      </div>
    </div>

    <!-- Headline -->
    <h2 class="fluid-h1 font-semibold text-zinc-900 mb-6">
      Prêt à passer <span class="text-zinc-500">de l'autre côté</span> ?
    </h2>

    <!-- Subheadline -->
    <p class="fluid-text-xl text-zinc-500 mb-10 font-light fluid-prose-xl mx-auto leading-relaxed">
      On identifie ensemble ce qui peut être automatisé — et on le fait.
    </p>

    <!-- CTA Button -->
    <div class="flex flex-col items-center gap-6">
      <CTAButton
        href="https://lilian.yt/contact"
        text="Discuter de mon projet"
        variant="primary"
        size="lg"
      />

      <!-- Badge -->
      <div class="flex items-center gap-1.5 fluid-text-xs text-zinc-500 mt-2">
        <span class="w-2 h-2 rounded-full bg-zinc-400 animate-pulse"></span>
        Créneaux disponibles cette semaine
      </div>
    </div>
  </div>
</section>
