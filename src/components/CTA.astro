---
import { Icon } from 'astro-icon/components';
---

<section id="contact" class="relative text-center py-24 md:py-32 px-6 overflow-hidden">
  <!-- Decorative background -->
  <div class="absolute inset-0 bg-gradient-to-b from-zinc-50 via-white to-zinc-50"></div>
  <div class="absolute top-0 left-1/4 w-96 h-96 bg-purple-200/20 rounded-full blur-3xl"></div>
  <div class="absolute bottom-0 right-1/4 w-96 h-96 bg-indigo-200/20 rounded-full blur-3xl"></div>

  <div class="relative max-w-3xl mx-auto">
    <!-- Headline -->
    <h2 class="text-3xl md:text-4xl lg:text-5xl font-semibold text-zinc-900 tracking-tight mb-6">
      Combien d'heures perdez-vous <span class="text-zinc-400">chaque semaine</span> ?
    </h2>

    <!-- Subheadline -->
    <p class="fluid-text-lg text-zinc-500 mb-10 font-light fluid-prose-xl mx-auto">
      On identifie ensemble ce qui peut être automatisé — et on le fait.<br />
      <strong class="text-zinc-700">Récupérez ces heures dès la semaine prochaine.</strong>
    </p>

    <!-- CTA Button -->
    <div class="flex flex-col items-center gap-6">
      <a
        href="https://cal.com/lilian-sevoumian/prise-de-brief"
        target="_blank"
        rel="noopener"
        class="group relative inline-flex items-center justify-center px-8 py-4 bg-zinc-900 text-white text-sm font-medium rounded-lg shadow-lg shadow-zinc-400/50 hover:shadow-xl hover:shadow-zinc-500/60 hover:-translate-y-0.5 transition-all overflow-hidden"
      >
        <span class="relative z-10 flex items-center gap-2">
          Me confier votre projet
          <Icon name="lucide:arrow-right" class="w-4 h-4" />
        </span>
        <div class="absolute inset-0 bg-zinc-800 transform scale-x-0 group-hover:scale-x-100 transition-transform origin-left duration-300"></div>
      </a>

      <!-- Badge -->
      <div class="flex flex-col sm:flex-row items-center gap-3 sm:gap-4 text-sm md:text-xs text-zinc-400 mt-2">
        <span class="flex items-center gap-1.5">
          <span class="w-2 h-2 rounded-full bg-green-500 animate-pulse"></span>
          Créneaux disponibles cette semaine
        </span>
        <span class="hidden sm:inline text-zinc-300">•</span>
        <span class="flex items-center gap-1.5">
          <Icon name="lucide:clock" class="w-4 h-4 md:w-3.5 md:h-3.5" />
          Réponse sous 24h
        </span>
      </div>
    </div>
  </div>
</section>
