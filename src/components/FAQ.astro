---
const faqs = [
  {
    question: "Qui est Lilian Sevoumian ?",
    answer: "Expert en automatisation no-code basé en France, spécialisé sur Make et n8n depuis 2021. Premier français certifié Make niveau 5 (top 5 mondial). J'ai accompagné plus de 100 entreprises et formé plus de 300 personnes. Je propose des formations, audits et projets d'automatisation sur-mesure."
  },
  {
    question: "Combien de temps dure un projet ?",
    answer: "Un processus simple (2-3 outils connectés) prend quelques heures. Un projet plus complexe (CRM, ERP, workflows multi-étapes) entre 1 et 4 semaines. Je travaille en sprints courts avec des livrables réguliers — pas de tunnel de 3 mois sans nouvelles."
  },
  {
    question: "J'ai une activité particulière, comment savoir si je peux automatiser ?",
    answer: "Si vous utilisez des outils numériques et que vous répétez des tâches manuellement, il y a de fortes chances qu'on puisse automatiser. Agence, e-commerce, startup, indépendant, association — j'ai travaillé avec tous les profils. Le plus simple : on en parle 15 minutes et je vous dis ce qui est faisable."
  },
  {
    question: "Comment vous contacter rapidement ?",
    answer: "Réservez un créneau directement dans mon agenda en ligne — c'est gratuit et sans engagement. Je réponds aussi par email sous 24h. Pas de formulaire à rallonge, pas d'attente commerciale."
  }
];

// FAQ Schema for SEO
const faqSchema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": faqs.map(faq => ({
    "@type": "Question",
    "name": faq.question,
    "acceptedAnswer": {
      "@type": "Answer",
      "text": faq.answer
    }
  }))
};
---

<section id="faq" class="py-24 px-6 bg-white">
  <div class="max-w-3xl mx-auto">
    <h2 class="fluid-h1 font-semibold text-zinc-900 mb-4">
      Questions fréquentes
    </h2>
    <p class="text-zinc-700 font-light fluid-text-xl mb-12 leading-relaxed">
      Les réponses aux questions que vous vous posez sûrement.
    </p>

    <div class="space-y-4">
      {faqs.map((faq, index) => (
        <details
          class="group bg-white border border-zinc-200 rounded-xl overflow-hidden faq-item"
        >
          <summary class="flex items-center justify-between gap-4 p-5 cursor-pointer list-none hover:bg-zinc-50 transition-colors">
            <h3 class="fluid-text-base font-medium text-zinc-900 text-left">
              {faq.question}
            </h3>
            <span class="shrink-0 text-zinc-500 group-open:rotate-180 transition-transform duration-300">
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="m6 9 6 6 6-6"/>
              </svg>
            </span>
          </summary>
          <div class="faq-content">
            <div class="px-5 pb-5 pt-0">
              <p class="text-zinc-700 fluid-text-sm leading-relaxed fluid-prose-lg">
                {faq.answer}
              </p>
            </div>
          </div>
        </details>
      ))}
    </div>
  </div>

  <!-- FAQ Schema -->
  <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />
</section>

<style>
  .faq-item {
    transition: border-color 0.2s ease;
  }

  .faq-item[open] {
    border-color: #a1a1aa;
  }

  .faq-content {
    display: grid;
    grid-template-rows: 0fr;
    transition: grid-template-rows 0.3s ease-out;
  }

  .faq-item[open] .faq-content {
    grid-template-rows: 1fr;
  }

  .faq-content > div {
    overflow: hidden;
  }
</style>
