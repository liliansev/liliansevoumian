---
const faqs = [
  {
    question: "Combien coûte un projet d'automatisation ?",
    answer: "Un coaching d'1h coûte 200€, un audit complet 500€, et un projet sur-mesure à partir de 1 500€. On peut commencer petit : l'idée n'est pas de tout automatiser d'un coup, mais de cibler ce qui a le plus d'impact sur votre quotidien. Le périmètre et le tarif sont définis ensemble après un premier échange gratuit."
  },
  {
    question: "Quelle différence avec un développeur ?",
    answer: "Un développeur code sur-mesure, ce qui prend plus de temps et coûte plus cher. J'utilise des outils No-Code (Make, n8n, Airtable) qui permettent de livrer plus vite, de modifier facilement, et que vos équipes peuvent comprendre et maintenir. C'est souvent 3 à 5 fois plus rapide qu'un développement classique."
  },
  {
    question: "Et si les automatisations ne fonctionnent pas ?",
    answer: "Elles fonctionneront. Je teste chaque scénario avant livraison et je reste disponible pendant 3 mois pour ajustements. Si un problème survient, je le corrige gratuitement."
  }
];

// FAQ Schema for SEO
const faqSchema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": faqs.map(faq => ({
    "@type": "Question",
    "name": faq.question,
    "acceptedAnswer": {
      "@type": "Answer",
      "text": faq.answer
    }
  }))
};
---

<section id="faq" class="py-24 px-6 bg-white">
  <div class="max-w-3xl mx-auto">
    <h2 class="fluid-h1 font-semibold text-zinc-900 mb-4">
      Questions fréquentes
    </h2>
    <p class="text-zinc-700 font-light fluid-text-xl mb-12 leading-relaxed">
      Les réponses aux questions que vous vous posez sûrement.
    </p>

    <div class="space-y-4">
      {faqs.map((faq, index) => (
        <details
          class="group bg-white border border-zinc-200 rounded-xl overflow-hidden faq-item"
          data-faq-item
        >
          <summary class="flex items-center justify-between gap-4 p-5 cursor-pointer list-none hover:bg-zinc-50 transition-colors">
            <h3 class="fluid-text-base font-medium text-zinc-900 text-left">
              {faq.question}
            </h3>
            <span class="shrink-0 text-zinc-500 group-open:rotate-180 transition-transform duration-300">
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="m6 9 6 6 6-6"/>
              </svg>
            </span>
          </summary>
          <div class="faq-content">
            <div class="px-5 pb-5 pt-0">
              <p class="text-zinc-700 fluid-text-sm leading-relaxed fluid-prose-lg">
                {faq.answer}
              </p>
            </div>
          </div>
        </details>
      ))}
    </div>
  </div>

  <!-- FAQ Schema -->
  <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />
</section>

<style>
  .faq-item {
    transition: border-color 0.2s ease;
  }

  .faq-item[open] {
    border-color: #a1a1aa;
  }

  .faq-content {
    display: grid;
    grid-template-rows: 0fr;
    transition: grid-template-rows 0.3s ease-out;
  }

  .faq-item[open] .faq-content {
    grid-template-rows: 1fr;
  }

  .faq-content > div {
    overflow: hidden;
  }
</style>
