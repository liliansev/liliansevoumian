<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="630" viewBox="0 0 1200 630">
  <!-- Background -->
  <rect width="1200" height="630" fill="#18181b"/>

  <!-- Subtle grid pattern -->
  <defs>
    <pattern id="grid" width="60" height="60" patternUnits="userSpaceOnUse">
      <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#27272a" stroke-width="1"/>
    </pattern>
  </defs>
  <rect width="1200" height="630" fill="url(#grid)" opacity="0.5"/>

  <!-- Logo LS -->
  <rect x="80" y="80" width="80" height="80" rx="12" fill="#fafafa"/>
  <text x="120" y="135" text-anchor="middle" font-family="system-ui, -apple-system, sans-serif" font-size="32" font-weight="600" fill="#18181b">LS</text>

  <!-- Main title -->
  <text x="80" y="280" font-family="system-ui, -apple-system, sans-serif" font-size="64" font-weight="600" fill="#fafafa">Expert Automatisation</text>
  <text x="80" y="360" font-family="system-ui, -apple-system, sans-serif" font-size="64" font-weight="600" fill="#71717a">&amp; No-Code</text>

  <!-- Subtitle -->
  <text x="80" y="440" font-family="system-ui, -apple-system, sans-serif" font-size="28" fill="#a1a1aa">Make · n8n · Airtable</text>

  <!-- URL -->
  <text x="80" y="560" font-family="ui-monospace, monospace" font-size="20" fill="#52525b">liliansevoumian.fr</text>
</svg>
